{
  "name": "trainy-app",
  "version": "0.5.1",
  "description": "Trainy: internship management platform connecting students, universities, and companies through one internship workflow.",
  "author": "Tiwat Lertchai",
  "license": "MIT",
  "private": true,
  "packageManager": "bun@1.4.0",
  "workspaces": [
    "./server",
    "./client",
    "./shared"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:client": "turbo dev --filter=client",
    "dev:server": "turbo dev --filter=server",
    "build": "turbo build",
    "build:client": "turbo build --filter=client",
    "build:server": "turbo build --filter=server",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "test": "turbo test",
    "postinstall": "turbo build --filter=shared --filter=server"
  },
  "keywords": [
    "bun",
    "hono",
    "react",
    "vite",
    "monorepo",
    "turbo"
  ],
  "devDependencies": {
    "bun-types": "^1.4.0",
    "turbo": "^2.10.12"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "hono": "^4.13.5"
  },
  "overrides": {
    "esbuild": "0.25.12"
  }
}
